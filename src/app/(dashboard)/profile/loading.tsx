import { ProfileSkeleton } from '@module_1/profiles/components/ProfileSkeleton'

/** UI que Next.js muestra mientras el perfil resuelve sus datos en el servidor. */
export default function Loading() {
  return <ProfileSkeleton />
}
